require './lib/config/node_extensions'

grammar DecimalNumerics
  rule expression
    decimal space ('+' / '-') space decimal <Expression>
  end

  rule decimal
    '-'? [0-9]+ '.' [0-9]+ <DecimalLiteral>
  end

  rule space
    [\s]*
  end
end
