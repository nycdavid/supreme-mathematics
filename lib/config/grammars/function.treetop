require './lib/config/node_extensions'

grammar Function
  include Numerics
  include PolynomialElements

  rule expression
    operand+ <Expression>
  end

  rule operator
    [\s]* (rational / decimal / integer) <OperatorLiteral>
  end
  
  rule variable
    [a-z] <Variable>
  end
end
